# 可选：本机配置与团队默认不一致时使用（不会提交到 Git）
# 复制此文件为 application-local.yml，仅覆盖需要修改的项

spring:
  datasource:
    username: root
    password: 你的MySQL密码

langchain4j:
  open-ai:
    chat-model:
      api-key: sk-你的OpenAI密钥
    streaming-chat-model:
      api-key: sk-你的OpenAI密钥
