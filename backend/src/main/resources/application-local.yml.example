# 本地配置示例：复制此文件为 application-local.yml 并填入真实值

spring:
  datasource:
    username: root
    password: 123456

  data:
    redis:
      host: localhost
      port: 6379

langchain4j:
  open-ai:
    chat-model:
      api-key: sk-你的OpenAI密钥
    streaming-chat-model:
      api-key: sk-你的OpenAI密钥
